#!/bin/sh

if [[ ! -f ~/.dotfiles.upd || $(date -r ~/.dotfiles.upd +%s) -lt $(date -d "-5 mins" +%s) ]]; then
    printf "!! Updating dotfiles !!\n"
    touch ~/.dotfiles.upd
    cd ~/.dotfiles
    git pull
    stow --adopt --no-folding .
fi
